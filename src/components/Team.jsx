import React from "react";
import { data } from "../restApi.json";// here data is the array we have created in restAPi.json
//always remember the syntax..by practicing we can easily learn or understand the syntax
const Team = () => {
  return (
    <section className="team" id="team">
      <div className="container">
        <div className="heading_section">
          <h1 className="heading">OUR TEAM</h1>
          <p>
            Meet out team members. At Laxwani Restaurant, our team is the heart and soul of our establishment!!
          </p>
        </div>
        <div className="team_container">
          {data[0].team.map((element) => {
            return (
              <div className="card" key={element.id}>
                <img src={element.image} alt={element.name} />
                <h3>{element.name}</h3>
                <p>{element.designation}</p>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  );
};

export default Team;
